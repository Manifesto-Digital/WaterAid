{#
/**
 * @file
 * Template to provide HTML for the field and has the label wrapped in a H2.
 *
 * Available variables:
 * - element: The field element.
 * - label: The label of the field.
 * - show_colon: TRUE if colon should be displayed after label.
 * - items: List of all the field items. Each item contains:
 *   - attributes: List of HTML attributes for each item.
 *   - content: The field item's content.
 */
#}

<div{{ attributes.addClass(classes) }}>
    {% if not label_hidden %}
        <{{ settings['label_element'] }} class="field-label-{{ element['#label_display']|clean_class }}">
            <div>{{- label }}</div>
        </{{ settings['label_element'] }}>
    {% endif %}

    <ul class="field__items">
        {% for item in items %}
            <li>{{ item.content }}</li>
        {% endfor %}
    </ul>
</div>
