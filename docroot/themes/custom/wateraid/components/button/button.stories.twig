{# web/themes/custom/wateraid/components/button.stories.twig #}
{% stories button with { title: 'wateraid/Atoms/Button' } %}

  {% story PrimaryLight with {
    name: 'PrimaryLight',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'primary',
      theme: 'light',
      classes: ['custom-class', 'another-class']
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme,
      classes
    }) }}
  {% endstory %}

  {% story PrimaryDark with {
    name: 'PrimaryDark',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'primary',
      theme: 'dark'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme
    }) }}
  {% endstory %}

  {% story SecondaryLight with {
    name: 'SecondaryLight',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'secondary',
      theme: 'light'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme
    }) }}
  {% endstory %}

  {% story SecondaryDark with {
    name: 'SecondaryDark',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'secondary',
      theme: 'dark'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme
    }) }}
  {% endstory %}

  {% story Donate with {
    name: 'Donate',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'donate',
      theme: '',
      icon: 'wa-utility-lock'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme,
      icon
    }) }}
  {% endstory %}

  {% story IconLight with {
    name: 'IconLight',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'icon',
      theme: 'light',
      icon: 'wa-utility-arrow'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme
    }) }}
  {% endstory %}

  {% story IconDark with {
    name: 'IconDark',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'icon',
      theme: 'dark',
      icon: 'wa-utility-arrow'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme
    }) }}
  {% endstory %}

  {% story Social with {
    name: 'Social',
    args: {
      text: 'Click to view more'|t,
      link: 'https://www.google.com'|t,
      type: 'social',
      theme: '',
      icon: 'facebook'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      type,
      theme,
      icon
    }) }}
  {% endstory %}

  {% story IconSmall with {
    name: 'IconSmall',
    args: {
      id: 'wa-close',
      link: '',
      text: 'Click to view more'|t,
      type: 'icon-small',
      theme: 'dark',
      icon: 'wa-utility-close'
    }
  } %}

    {{ include('wateraid:button', {
      text,
      link,
      id,
      type,
      theme,
      icon
    }) }}
  {% endstory %}

  {% story ButtonWithClasses with {
    name: 'ButtonWithClasses',
    args: {
      id: 'custom-button',
      text: 'Button with custom classes'|t,
      type: 'primary',
      theme: 'light',
      classes: ['js-custom-button', 'has-tooltip']
    }
  } %}

    {{ include('wateraid:button', {
      text,
      id,
      type,
      theme,
      classes
    }) }}
  {% endstory %}

{% endstories %}
