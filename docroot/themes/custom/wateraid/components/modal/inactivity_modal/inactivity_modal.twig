{% set modal_classes = [
  'inactivity-modal'
] %}

<div {{ attributes.addClass(modal_classes) }}>
  <div class="inactivity-modal__wrapper">
    <div class="inactivity-modal__image">
      {{ include('wateraid:image', {
        image: image,
        caption: caption,
        credit: credit,
        width: 'full',
        caption_display: 'expandable'
      }) }}
    </div>


    <div class="inactivity-modal__content">
      <div class="inactivity-modal__close">
        {# attributes here is inherited in the close button from attributes above #}
        {% set attributes = attributes.removeClass('inactivity-modal') %}
        {{ include('wateraid:button', {
          text: 'Close',
          link: '',
          id: 'modal-close',
          type: 'icon-small',
          icon: 'wa-utility-close',
          theme: 'dark',
        }) }}
      </div>
      <h2 class="inactivity-modal__title">{{ title }}</h2>

      <div class="inactivity-modal__copy">
        {{ copy }}
      </div>

      <div class="inactivity-modal__cta">
        {% if cta %}
          {{ include('wateraid:button', {
            text: cta.title,
            link: cta.url,
            type: 'donate'
          }) }}
        {% elseif continueDonation %}
          {{ include('wateraid:button', {
            text: 'Continue my donation',
            link: '#',
            classes: ['continue-donation-button'],
            type: 'donate',
          }) }}
        {% endif %}
      </div>
    </div>
  </div>
</div>
