{#
/**
 * @file
 * Theme override to display the footer block.
 *
 * Available variables:
 * - plugin_id: The ID of the block implementation.
 * - label: The configured label of the block if visible.
 * - configuration: A list of the block's configuration values.
 *   - label: The configured label for the block.
 *   - label_display: The display settings for the label.
 *   - provider: The module or other provider that provided this block plugin.
 *   - Block plugin specific settings will also be stored here.
 * - content: The content of this block.
 * - attributes: array of HTML attributes populated by modules, intended to
 *   be added to the main container tag of this template.
 *   - id: A valid HTML ID and guaranteed unique.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @see template_preprocess_block()
 */
#}
{# Footer variables #}
{% set facebook = config_pages_field('site_settings', 'field_facebook').0['#title'] %}
{% set instagram = config_pages_field('site_settings', 'field_instagram').0['#title'] %}
{% set tiktok = config_pages_field('site_settings', 'field_tiktok').0['#title'] %}
{% set linkedin = config_pages_field('site_settings', 'field_linkedin').0['#title'] %}
{% set youtube = config_pages_field('site_settings', 'field_youtube').0['#title'] %}
{% set cta = {
  link: config_pages_field('footer', 'field_donate_link').0['#url'],
  text: config_pages_field('footer', 'field_donate_link').0['#title']
} %}
{% set contact_details = {
  tel: config_pages_field('footer', 'field_phone_number')|render|striptags|trim,
  mail: config_pages_field('footer', 'field_email')|render|striptags|trim,
  address: config_pages_field('footer', 'field_address'),
} %}
<div{{ attributes }}>
  {{ include('wateraid:footer', {
    primary_menu: footer_primary_menu ? footer_primary_menu['#items'] : [],
    secondary_menu: footer_secondary_menu ? footer_secondary_menu['#items'] : [],
    facebook_url: facebook|render ? facebook,
    instagram_url: instagram|render ? instagram,
    tiktok_url: tiktok|render ? tiktok,
    linkedin_url: linkedin|render ? linkedin,
    youtube_url: youtube|render ? youtube,
    cta: cta,
    contact_details: contact_details,
    copy: config_pages_field('footer', 'field_copy'),
    logos: config_pages_field('footer', 'field_logos'),
    site_logo: config_pages_field('site_settings', 'field_site_logo'),
    variant: 'default'
  }, only) }}
</div>

{% set catch_cache = content|render %}
{{ footer_primary_menu|without(['#items']) }}
{{ footer_secondary_menu|without(['#items']) }}
