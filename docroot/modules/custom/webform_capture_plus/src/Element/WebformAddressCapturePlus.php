<?php

namespace Drupal\webform_capture_plus\Element;

use Drupal\webform\Element\WebformAddress;

/**
 * Provides a 'webform_address_capture_plus' element.
 *
 * WebformElement assumed an Element with the same ID. However, we do not need
 * to make any changes to the WebformAddress base class so this class can remain
 * empty.
 *
 * @FormElement("webform_address_capture_plus")
 */
class WebformAddressCapturePlus extends WebformAddress {

}
