const Cropper = require('cropperjs');

(function (Drupal) {
  'use strict';

  Drupal.behaviors.wateraidContentImageFocalPoint = {
    attach: function (context, settings) {
      console.log('Focal Point JS loaded');
      // create image with src
      const elem
      // https://dam-cdn.wi0.orangelogic.com/AssetLink/8w21j2ih7s4e5q5e504s15n766rb8tbw-1440x480,CentreCrop.jpg
    }
  };

})(Drupal);
